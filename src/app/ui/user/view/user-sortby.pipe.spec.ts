import { UserSortbyPipe } from './user-sortby.pipe';

describe('UserSortbyPipe', () => {
  it('create an instance', () => {
    const pipe = new UserSortbyPipe();
    expect(pipe).toBeTruthy();
  });
});
