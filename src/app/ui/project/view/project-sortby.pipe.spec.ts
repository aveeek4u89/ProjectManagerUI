import { ProjectSortbyPipe } from './project-sortby.pipe';

describe('ProjectSortbyPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectSortbyPipe();
    expect(pipe).toBeTruthy();
  });
});
